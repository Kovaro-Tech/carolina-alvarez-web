import { Link } from 'react-router-dom'
import { practiceAreas } from '../data/practiceAreas'
import Icon from '../components/Icon'
export default function Services() { return <><PageIntro eyebrow="Áreas de práctica" title={<>Soluciones jurídicas según las necesidades de cada <em>caso.</em></>} text="Una orientación jurídica rigurosa comienza por comprender la situación y sus particularidades." /><section className="section services-detail"><div className="container">{practiceAreas.map(area => <article className="area-detail" key={area.number}><span>{area.number}</span><div><h2>{area.title}</h2><p>{area.text}</p><p className="placeholder-note">El detalle de servicios específicos de esta área se incorporará cuando sea confirmado por Carolina.</p><Link className="text-link" to="/contacto">Consultar sobre esta área <Icon name="arrow" /></Link></div></article>)}</div></section></> }
export function PageIntro({ eyebrow, title, text }) { return <section className="page-intro"><div className="container"><p className="eyebrow"><span /> {eyebrow}</p><h1>{title}</h1>{text && <p>{text}</p>}</div></section> }
